{
  "plugins": {
    "@j-ulrich/release-it-regex-bumper": {
      "in": "VERSION",
      "out": {
        "files": ["VERSION", "src/**/*.omn", "src/**/*.xml", "src/**/*.owl"],
        "pattern": "v{{latestVersion}}",
        "replace": "{{version}}"
      }
    }
  },
  "github": {
    "release": true
  }
}
